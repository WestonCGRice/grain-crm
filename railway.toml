[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npx tsx prisma/seed-admin.ts && next start -p ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
